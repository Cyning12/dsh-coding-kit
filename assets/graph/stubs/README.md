# graph/stubs

产品包 **不内置** 某上游 OSS 的预填图谱。

- 默认 bootstrap：仅用 [`../templates/`](../templates/)（通用占位）+ 删除 `10_flow_MAIN*`。
- 项目专用快照：见 [`../../examples/oss-fork/`](../../examples/oss-fork/)，通过 `--stub-dir` 显式加载。
